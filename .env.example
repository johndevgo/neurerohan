# Server-only Resend credentials for the contact form.
# Only RESEND_API_KEY is required when neurerohan.com.np is verified in Resend.
# Never prefix secrets with NEXT_PUBLIC_ and never commit real values.
RESEND_API_KEY=re_replace_with_your_private_key
CONTACT_TO_EMAIL=contact@neurerohan.com.np
